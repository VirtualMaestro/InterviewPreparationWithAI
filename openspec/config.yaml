# OpenSpec Project Config — InterviewPreparationWithAI_Kiro
# Auto-generated by /migrate-to-openspec — Review and adjust as needed
# This context is injected into every artifact (proposal, specs, design, tasks)

schema: spec-driven

context: |
  Project: InterviewPreparationWithAI_Kiro
  Type: AI-Powered Web Application (Interview Preparation Tool)
  Status: Near Complete (14/16 tasks done, fully functional)

  Tech stack:
  - Python 3.11+ (modern syntax enforced: PEP 585/604/612, async/await, match-case)
  - Streamlit 1.49.1+ (web UI framework with session state management)
  - OpenAI API 1.106.1+ (GPT-4o, GPT-5 models for question generation)
  - Tenacity 9.1.2+ (retry logic with exponential backoff)
  - pytest 8.4.2+ with pytest-asyncio (100+ tests, 100% pass rate)

  Architecture: Modular Layered Architecture with Global Singleton Instances
  - UI Layer: Streamlit GUI (app.py)
  - Business Logic: AI Engine (src/ai/) with 62 prompt templates across 5 techniques
  - Infrastructure: Utilities (src/utils/) for cost, security, rate limiting, error handling
  - External Service: OpenAI API with async integration

  Key systems:
  - Question Generation: Personalized interview questions (1-20 questions, 4 interview types, 4 experience levels)
  - Prompt Engineering: 5 techniques (Few-Shot, Chain-of-Thought, Zero-Shot, Role-Based, Structured Output)
  - Mock Interview Mode: Interactive Q&A with answer evaluation and feedback
  - Cost Tracking: Real-time token-based calculation with 6-decimal precision
  - Security: Input validation, prompt injection protection (75% attack blocking rate)
  - Rate Limiting: Sliding window algorithm (100 calls/hour default)

  External dependencies:
  - OpenAI API (gpt-4o, gpt-4o-mini, gpt-5)
  - Environment variables: OPENAI_API_KEY, DEBUG, MAX_INPUT_LENGTH, RATE_LIMIT_CALLS

  Testing: Comprehensive test suite (30+ files, 100+ tests)
  - Unit tests: test_*_simple.py pattern
  - Integration tests: test_*_integration.py pattern
  - System tests: test_complete_system.py
  - Security tests: security_demo.py with attack simulation

  Conventions discovered:
  - Modern Python 3.11+ syntax required (no legacy patterns: typing.List, bare except, .format())
  - Dataclass models preferred (simple_schemas.py) over Pydantic (schemas.py has import issues)
  - Global singleton instances: prompt_library, cost_calculator, rate_limiter, SecurityValidator
  - Template auto-registration pattern for prompt library
  - Multi-level fallback system: Primary → Secondary → Zero-shot → Emergency defaults
  - Async/await for all OpenAI API calls with tenacity retry logic
  - F-strings exclusively for string formatting
  - Type hints with list[], dict[] (not List[], Dict[])

  Known constraints:
  - Python 3.11+ minimum version (modern syntax enforcement)
  - OpenAI API rate limits and costs (monitored via cost_calculator)
  - GPT-5 support currently disabled in UI (buggy, under investigation)
  - Config defaults to GPT_5 but app.py overrides to GPT_4O (config mismatch)
  - No automatic code formatting configured (black/flake8/ruff not in dependencies)
  - Stateless application (no database, session data in Streamlit session_state)

rules:
  proposal:
    - Always describe impact on existing systems (14 features identified)
    - Reference the spec file(s) being modified
    - Consider impact on global singletons (prompt_library, cost_calculator, rate_limiter)
    - Include rollback plan for AI prompt template changes
    - List all affected modules and their dependencies
    - Always include an "## Alternatives Considered" section listing at least 2 alternative approaches with their pros, cons, and reason for rejection. Format each as: "### Option: <name> / Pros: ... / Cons: ... / Why rejected: ..."
  specs:
    - Use Given/When/Then format for all scenarios
    - Each Requirement must have at least one Scenario
    - Reference actual file paths from src/ directory
    - Include security and cost tracking considerations
  design:
    - Must reference at least one existing file by path
    - Must describe impact on other systems (use feature dependency map)
    - Respect modern Python 3.11+ syntax requirements
    - Consider fallback strategies for AI operations
    - Document token costs and rate limit implications
  tasks:
    - Keep each task scoped to 1-3 files maximum
    - Order tasks so earlier ones never depend on later ones
    - Include test file creation/updates in task scope
    - After all tasks complete, run /opsx:verify
